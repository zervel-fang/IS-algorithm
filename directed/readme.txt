Here, we take the twitter1 that generated by configuration model as the test dataset.

Note 1: If you run the program in linux system, you should complie it again.
Note 2: the program can run in parellel when complie it with the command: gcc -fopenmp -o main main.c
Note 3: if you don't want to run it in parallel, use the command: gcc -o main main.c. 
Note 4: For incompatible reason, you may also need to specify the version of  C, with the command: gcc -std=c99 -fopenmp -o main main.c 
